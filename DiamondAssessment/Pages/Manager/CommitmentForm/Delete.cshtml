@page
@model DiamondAssessment.Pages.Manager.CommitmentForm.DeleteModel
@{
    ViewData["Title"] = "Delete";
    var role = HttpContext.Session.GetString("Role");
    if (role is not "Manager") Response.Redirect("/Error");
}
<h1>Rejecting</h1>

<h3>Are you sure you want to reject this?</h3>
<div>
    <h4>Commitment Form</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CommitmentForm.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CommitmentForm.Id)
        <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CommitmentForm.CreatedAt)
        </dt>
        <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CommitmentForm.CreatedAt)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="CommitmentForm.Id" />
        <input type="submit" value="Reject" class="btn btn-danger" /> |
        <a asp-page="/Manager/CommitmentForm/Index">Back to List</a>
    </form>
</div>
