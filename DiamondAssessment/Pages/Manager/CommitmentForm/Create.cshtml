@page
@using Entities.Models.Enum
@using NuGet.Protocol
@model DiamondAssessment.Pages.Manager.CommitmentForm.CreateModel
@{
    ViewData["Title"] = "Create";
    Layout = "Shared/_Layout";
    var role = HttpContext.Session.GetString("Role");
    if (role is not "Manager") Response.Redirect("/Error");
}
<h1>Create <strong class="text-warning">Commitment Form</strong></h1>

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CommitmentForm.CreatedAt" class="control-label"></label>
                <input asp-for="CommitmentForm.CreatedAt" class="form-control" />
                <span asp-validation-for="CommitmentForm.CreatedAt" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="/Manager/CommitmentForm/Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
